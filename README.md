# Some scripts to automate tasks in NetworkManager

## Install
* Clone the repository
* symlink or put the desired scripts into /etc/NetworkManager/dispatcher.d.
* Add execution rights to the scripts with `chmod +x SCRIPTNAME`



